import 'package:flutter/material.dart';

class TextLook {
  TextStyle normalText(double size, Color color) => TextStyle(
        fontFamily: 'FiraCode',
        color: color,
        fontSize: size,
      );
}
